# Watch Compass

Day 1 scaffold: solution, projects, and folder structure only (no features yet).

## Getting started
- Prerequisite: .NET SDK 10.0.100 (see `global.json`).
- Restore/build: `dotnet build`.
- Run API placeholder: `dotnet run --project src/WatchCompass.Api`.
