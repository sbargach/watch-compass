# Watch Compass Frontend

React + TypeScript client for `Watch Compass`.

## Run
- Install dependencies: `npm install`
- Start dev server: `npm run dev`
- Build: `npm run build`

## Environment
- `VITE_API_BASE_URL` (optional): defaults to `http://localhost:5276`
